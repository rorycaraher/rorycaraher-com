2026-06-25: Changed data-theme="light" to data-theme="dark" in themes/rorycaraher/layouts/_default/baseof.html. First step of the dark theme redesign.
2026-06-25: Updated --mg-color-initial in the dark block of themes/rorycaraher/assets/css/mgplus.css from #302f2f (warm/brown) to #18181b (neutral near-black).
2026-06-25: Added --mg-color-primary: #b175d6 override in the dark theme block of mgplus.css. The base purple (#9b4dca) only achieves ~3.4:1 contrast on #18181b (fails WCAG AA); the lightened value (#b175d6, same hue at ~65% lightness) reaches ~5.0:1 and passes AA for normal text.
2026-06-25: Uncommented the nav menu block in themes/rorycaraher/layouts/partials/header.html. The `<div class="mg-nav mg-nav--inline">` wrapping `{{ partial "menu.html" ... }}` was commented out; removing the comment wrapper enables the navigation bar to render once menu entries are added to hugo.toml.
2026-06-25: Added [[menus.main]] entries to hugo.toml for Home (/), Projects (/projects/), and Music (/music/) with weights 1, 2, 3 respectively. The nav bar will now render these three links once Hugo builds the site.
2026-06-25: Fixed active-page highlighting in themes/rorycaraher/layouts/partials/menu.html. The CSS targets .mg-nav ul > li.active but the partial was putting class="active" on the <a> tag; moved it to the <li> element so the bottom-border accent renders on the active nav item. aria-current="page" remains on the <a> for accessibility.
2026-06-25: Created data/projects.yaml with name, description, url, tags, and featured fields. Seeded with 3 featured entries (rorycaraher-com, getolo role, dotfiles). This is the data source for the Projects page card grid.
2026-06-25: Created themes/rorycaraher/layouts/projects/list.html — card grid layout using mg-row and mg-x12/mg-m4 columns (full width on mobile, 3-up on medium+ screens). Iterates site.Data.projects filtered to featured: true, rendering each as a bordered card with name (linked), description, and tag pills using CSS variables from mgplus.css.
2026-06-25: Added Projects teaser section to themes/rorycaraher/layouts/_default/home.html. Shows up to 3 featured project cards (reusing the same card structure as the projects list page) below the homepage content, with a "See all projects →" link pointing to /projects/.
2026-06-25: Created content/music/_index.md — intro text and per-project write-ups for the Music page. Intro describes the corner_nltl alias and sound; includes an embedded SoundCloud iframe player pointing to the user's SoundCloud profile.
2026-06-25: Created themes/rorycaraher/layouts/music/single.html — a minimal content layout (title + .Content, no date) for individual music entry pages. Omits the blog-style date timestamp that _default/single.html renders, since music pages are not dated posts.
2026-06-25: Created themes/rorycaraher/layouts/shortcodes/soundcloud.html — Hugo shortcode accepting url and optional height params; renders the SoundCloud player iframe with the site's purple accent (#b175d6). Updated content/music/_index.md to replace the raw iframe with {{< soundcloud url="..." >}} so embeds are clean and reusable across any markdown file.
2026-06-25: Restructured content/_index.md — moved bio text and social links (github, soundcloud) into TOML front matter fields (bio, github, soundcloud) so the home layout can render them separately from the work history. The markdown body now contains only the Work section. This is PRD task 5.1.
2026-06-25: Updated themes/rorycaraher/layouts/_default/home.html (PRD task 5.2) — added bio section at top (renders .Params.bio with GitHub/SoundCloud links), kept featured projects grid, added music teaser section linking to /music/, added conditional blog teaser (only renders if posts exist via Hugo if guard), and moved .Content (work history) to a bottom section.
2026-06-25: Wrapped work history section in a <details>/<summary> element in home.html (PRD task 5.3) — the CV-style work list now collapses by default so it doesn't dominate the homepage above the bio, projects, and music sections.
2026-06-25: Implemented PRD section 6 (Footer) — populated themes/rorycaraher/layouts/partials/footer.html with a copyright line (site.Copyright from hugo.toml) and GitHub/SoundCloud text links. Added [params] block to hugo.toml with github and soundcloud URLs so the footer can reference them as site.Params rather than duplicating the values.
2026-06-25: Confirmed PRD task 7.1 — themes/rorycaraher/layouts/_default/list.html and single.html both exist and are correctly wired for posts. list.html renders page list with title, link, and summary via {{ define "main" }}; single.html renders title, date, content, and taxonomy terms. baseof.html provides the {{ block "main" }} slot both fill, and the terms.html partial referenced in single.html exists.
2026-06-25: Created archetypes/posts.md (PRD task 7.2) — front matter defaults for new posts: title (auto-generated from filename), date, draft: true, description, and tags. This archetype is used by `hugo new posts/<slug>.md` to scaffold new blog posts with sensible defaults.
2026-06-25: Implemented conditional Blog nav entry (PRD task 7.3) — added Blog menu entry to hugo.toml with identifier "blog" and weight 4. Updated themes/rorycaraher/layouts/partials/menu.html to skip rendering any menu entry with identifier "blog" when no published posts exist in the posts section (checked via `where site.RegularPages "Section" "posts"`). The Blog link will automatically appear in the nav once at least one post is published.
2026-06-25: Verified PRD task 7.4 (homepage blog teaser if guard) — the conditional block was already implemented as part of PRD task 5.2. themes/rorycaraher/layouts/_default/home.html lines 38-52 use `{{ $posts := where site.RegularPages "Section" "posts" }}` and `{{ if $posts }}` to gate the Writing teaser section; it only renders when at least one published post exists. Checked off the final PRD checkbox.
STATUS: COMPLETE
